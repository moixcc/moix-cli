# Moix Client

Create a compressed (`brotli`) version of index.html with svg, javascript, css and embedded templates.

```bash
# moix OPTION PATH
moix init ./test
moix build ./test
moix dev ./test
# http://localhost:8080
```

